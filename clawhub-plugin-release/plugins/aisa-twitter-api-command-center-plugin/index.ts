const plugin = {
  id: "aisa-twitter-api-command-center-plugin",
  name: "AIsa Twitter API Command Center",
  register() {
    // Native wrapper only. The packaged skill payload is declared in openclaw.plugin.json.
  },
};

export default plugin;
