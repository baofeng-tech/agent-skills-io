const plugin = {
  id: "twitter-command-center-search-post-interact-plugin",
  name: "Twitter Command Center Search Post Interact",
  register() {
    // Native wrapper only. The packaged skill payload is declared in openclaw.plugin.json.
  },
};

export default plugin;
